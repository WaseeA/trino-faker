project:
  name: basic-project
  description: This is a Trino connector
  license: Apache-2
  java:
    groupId: archetype.it
    version: 21

files:
  artifacts:
    - path: "target/{{projectName}}-{{projectVersion}}.zip"

release:
  github:
    owner: nineinchnick
